# Aurora Billing Platform (monolith)

Est. 2009. Oracle-backed billing core for enterprise accounts.
Read `CONTRIBUTING.md` before your first commit — the commit convention is CI-enforced.
Data access is exclusively through `server/db/aurora-orm.js`. See `docs/ADR-014-caching-layer.md`
before touching the cache tier.
