# CLAUDE.md

> **Generated by the Enterprise Agentic Bridge.** Do not edit by hand — re-run
> `synthesize_claude_md` instead. Every claim below was extracted from this repository
> or from a decision a human explicitly validated; evidence paths are cited inline.

- **Target:** `/Users/himanshumudigonda/Projects/nitrostack_project/agentic-bridge/fixtures/legacy-monolith`
- **Generated:** 2026-07-25T12:45:00.127Z
- **Knowledge facts:** 57
- **Generated skills:** 1
- **Conflicts:** 1 detected, 1 resolved by a human
- **Source run:** `run-1-ms0d37dy` (completed)

## 0. Authoritative decisions (human-validated)

These override anything else in this document, in the ticket tracker, or in the code.
Each one is a conflict between written and spoken sources that an administrator
personally adjudicated.

### redis, memcached, cache

**Ruling:** Authoritative: Microsoft Teams (Microsoft Teams — Aurora Billing / Sprint 41 Daily Scrum 10:03 K. Brandt). Ops will not approve Redis in the PCI zone. That was ADR-014 in 2023 and nothing has changed. The shared cache hosts are capped at 4 gigs RSS and Redis does not fit next to what is already resident.

<sub>Resolved by `verify-script` at 2026-07-25T12:45:00.107Z. Conflict was `contradiction` — alignment 0.2073, divergence 1. Sources: Jira (AUR-4471) vs Microsoft Teams.</sub>

## 1. Architectural topography

**Layer distribution:** config: 9 file(s), docs: 5 file(s), ui-component: 3 file(s), service: 3 file(s), other: 2 file(s), test: 2 file(s), route: 2 file(s), middleware: 2 file(s), data-access: 2 file(s), batch: 2 file(s), ui-view: 1 file(s)

```
├── .github/
│   └── workflows/
│       └── pr-checks.yml
├── docs/
│   ├── ADR-014-caching-layer.md
│   └── architecture-overview.md
├── scripts/
│   ├── check-coverage.js
│   └── reconcile_nightly.py
├── server/
│   ├── config/
│   │   └── database.json
│   ├── db/
│   │   ├── aurora-orm.js
│   │   └── connection-pool.js
│   ├── middleware/
│   │   ├── auth.js
│   │   └── cache.js
│   ├── routes/
│   │   ├── customers.route.js
│   │   └── invoices.route.js
│   ├── services/
│   │   ├── audit.service.js
│   │   ├── customer.service.js
│   │   └── invoice.service.js
│   └── index.js
├── src/
│   └── test/
│       ├── invoice.service.spec.js
│       └── setup.js
├── web/
│   └── src/
│       ├── components/
│       │   ├── DataTable.jsx
│       │   ├── MoneyCell.jsx
│       │   └── StatusPill.jsx
│       ├── styles/
│       │   └── tokens.css
│       └── views/
│           └── InvoiceListView.jsx
├── .eslintrc.json
├── commitlint.config.js
├── CONTRIBUTING.md
├── Jenkinsfile
├── jest.config.js
├── package.json
├── pom.xml
├── README.md
├── requirements.txt
└── tailwind.config.js
```

### Files with the widest blast radius

Changing any of these requires reviewing every dependent. Use `find_change_surface`
before you touch one.

- server/db/aurora-orm.js (data-access) is imported by 3 other file(s). Changing it requires reviewing every dependent.
- server/middleware/auth.js (middleware) is imported by 2 other file(s). Changing it requires reviewing every dependent.
- server/services/invoice.service.js (service) is imported by 2 other file(s). Changing it requires reviewing every dependent.
- server/config/database.json (config) is imported by 1 other file(s). Changing it requires reviewing every dependent.
- server/db/connection-pool.js (data-access) is imported by 1 other file(s). Changing it requires reviewing every dependent.

## 2. Available tailored skills

The swarm generated these MCP tools specifically for this codebase. Prefer them over
reading source files by hand — they encode how this project actually works.

### `query_aurora_orm_usage`

Finds every AuroraORM call site and reports which tables the codebase touches.

_No parameters._

**Why it exists:** AuroraORM is a hand-rolled 2009 mapper with no modern equivalent.
<sub>Source: `src/skills/query_aurora_orm_usage.skill.ts`</sub>

## 3. Current human alignment

**Active sprint: Sprint 41 - Invoice Read Path.** Goal: Cut p95 latency on the invoice list endpoint below 400ms.. Runs 2026-07-20T09:00:00Z → 2026-08-03T17:00:00Z. 3 of 4 issues still open.

### In flight right now — do not duplicate this work

- **AUR-4471 (In Progress) — Introduce a Redis cache in front of the invoice read path**  
  Stand up Redis as the caching layer for GET /api/customers/:id/invoices. Replace the Memcached wrapper in server/middleware/cache.js with a Redis client. Target a 5 minute TTL keyed by customer and date window. Owner: p.narang.
- **AUR-4472 (To Do) — Add pagination to the customer tier listing**  
  server/services/customer.service.js findByTier already pages at 50. Expose page size through the route and the DataTable component. Owner: m.olawale.
- **AUR-4480 (In Review) — Null ISSUED_ON crashes the invoice date-window query**  
  AuroraORM between() emits a bind for null and Oracle throws ORA-01008. Guard in invoice.service.js. Owner: r.whitfield.

### Verbal decisions not reflected in the tracker

_Source: Human consensus: Microsoft Teams — Aurora Billing / Sprint 41 Daily Scrum. Tuesday, 22 July 2026, 10:00–10:22 (BST). 6 attendees. 14 binding directive(s) from 15 utterances._

- **REJECT: redis, cache, pci — K. Brandt**  
  "Ops will not approve Redis in the PCI zone. That was ADR-014 in 2023 and nothing has changed. The shared cache hosts are capped at 4 gigs RSS and Redis does not fit next to what is already resident." (10:03, authority: ops)
- **REJECT: redis, memcached — D. Fairbanks**  
  "Right. So the decision is: we are NOT introducing Redis. We stay on Memcached and we optimise the existing wrapper instead. P., can you retitle AUR-4471?" (10:04, authority: lead)
- **REJECT: redis, memcached — D. Fairbanks**  
  "Correct. Memcached stays. Nobody writes a Redis client into this repo." (10:06, authority: lead)
- **REJECT: pci — K. Brandt**  
  "And no second datastore in the PCI zone, to be explicit for whoever reads this later." (10:07, authority: ops)
- **FREEZE: aurora-orm, orm, aurora-orm.js — D. Fairbanks**  
  "Second thing. We are freezing changes to aurora-orm.js for the rest of the sprint." (10:08, authority: lead)
- **FREEZE: aurora-orm, orm, aurora-orm.js — D. Fairbanks**  
  "Say it plainly: aurora-orm.js is frozen. Fixes go in the service layer." (10:10, authority: lead)
- **REJECT: graphql — D. Fairbanks**  
  "Last one — we are NOT doing the GraphQL gateway this quarter. It keeps coming up in planning. It is off the roadmap until at least Q1. Do not start it." (10:18, authority: lead)
- **MANDATE — D. Fairbanks**  
  "Noted. Reviewers, reject ad-hoc colours." (10:13, authority: lead)
- **MANDATE: commitlint — D. Fairbanks**  
  "That stays. Also every commit needs the AUR key in brackets or commitlint rejects it." (10:16, authority: lead)
- **REJECT: redis, memcached, cache — P. Narang**  
  "I can, but I already started the Redis client swap on the branch. I'll revert it. So the work becomes: keep server/middleware/cache.js on Memcached, add a proper key namespace, and raise the TTL from 60 to 300 seconds on the invoice path." (10:05, authority: engineer)

## 4. Cultural and syntactic rigor

### Commit messages

Format: `<type>(<scope>): <subject>   [TICKET-KEY]`. Types: feat, fix, perf, refactor, test, chore, docs. Scopes: billing, invoice, customer, orm, web, ci, batch. A ticket key is MANDATORY — CI rejects commits without one. Header max 90 chars. Example: `fix(invoice): guard against null ISSUED_ON in date window [AUR-4471]`

<sub>Evidence: `commitlint.config.js`</sub>

### Testing

```
Jest (node) — Jest, environment=node, coverage collection configured
JUnit 4 (java) — JUnit 4 — use @Test(expected=...) and @Before, NOT Jupiter annotations
Mockito (java) — Mocking library for the Java tier
pytest (python) — pytest for the batch tier
```

- Convention: <name>.spec.js — the dominant convention here. Specs live in: src/test (1).
- **The build FAILS below these thresholds: branches 70%, functions 75%, lines 78%, statements 78%**

### Code style (CI-enforced)

- **Lint rules (.eslintrc.json)**: extends eslint:recommended. quotes=["error","single"], semi=["error","always"], indent=["error",2], no-var="off", camelcase=["error",{"properties":"never"}], max-len=["warn",{"code":120}]

### Written engineering policy

- Hotfixes branch from `master` as `hotfix/AUR-<ticket>-<slug>` and must be back-merged to `develop`.  _(CONTRIBUTING.md)_
- ## Commit message convention (enforced by commitlint in CI)  _(CONTRIBUTING.md)_
- The Jira key in square brackets is **mandatory** — CI rejects commits without it.  _(CONTRIBUTING.md)_
- Node: Jest. Specs live in `src/test/*.spec.js`. Mock `aurora-orm`, never hit Oracle in unit tests.  _(CONTRIBUTING.md)_
- `server/db/` are rejected in review. The ORM is callback-based; do not wrap it in promises  _(CONTRIBUTING.md)_

### Delivery pipeline

- **jenkins pipeline (Jenkinsfile)** — Stages in order: Checkout → Lint → Unit Tests → Coverage Gate → Build → Deploy to STAGE → Deploy to PROD. Runs on: aurora-build-rhel8. Uses secrets: aurora-oracle-prod.
- **github-actions pipeline (.github/workflows/pr-checks.yml)** — Stages in order: verify: npm ci → verify: npm run lint → verify: npm test -- --coverage → Enforce commit convention. Runs on: ubuntu-latest.
- **Branch model** — develop: staging (Deploy to STAGE); master: hotfix branch naming pattern; feature/AUR-<ticket>-<slug>: feature branch naming pattern
- **Approval gates** — "Deploy to PROD" in Jenkinsfile requires approval from release-managers

### Dependency reality

- npm: 12 package(s), maven: 4 package(s), pypi: 5 package(s)
- **Pinned behind current practice — do not generate code against newer APIs:**
  - `eslint@7.32.0` — ESLint 7/8 — legacy .eslintrc config format, not flat config
  - `express@4.17.1` — Express 4.x — middleware signature and error handling differ from 5.x
  - `jest@26.6.3` — Jest 26 — no native ESM support, older matcher set
  - `junit:junit@4.13.2` — JUnit 4 — @Test(expected=) style, not Jupiter
  - `moment@2.29.1` — moment is in maintenance mode upstream
  - `react@16.14.0` — React 16/17 — no concurrent features, class components likely present
  - `tailwindcss@2.2.19` — Tailwind 2 — no JIT-by-default, different config surface
  - `webpack@4.46.0` — Webpack 4 — different loader/plugin API from 5.x

## 5. Design system adherence

### Colour

```
Use ONLY these colour tokens. Never introduce a raw hex value:
  aurora-navy = #0B2545
  aurora-slate = #134074
  aurora-mist = #8DA9C4
  aurora-cloud = #EEF4ED
  aurora-signal = #C1440E
  aurora-ok = #1B7F5F
  --aur-color-navy = #0B2545
  --aur-color-slate = #134074
  --aur-color-mist = #8DA9C4
  --aur-color-cloud = #EEF4ED
  --aur-color-signal = #C1440E
  --aur-color-ok = #1B7F5F
```

### Typography

```
sans: Inter Tight, Helvetica Neue, sans-serif
mono: IBM Plex Mono, monospace
--aur-font-sans: 'Inter Tight', 'Helvetica Neue', sans-serif
--aur-font-mono: 'IBM Plex Mono', monospace
```

### Spacing, radius, motion

```
gutter = 18px
card = 2px
--aur-space-gutter = 18px
--aur-space-stack = 12px
--aur-radius-card = 2px
--aur-elevation-1 = 0 1px 2px rgba(11, 37, 69, 0.12)
--aur-duration-fast = 120ms
```

### Component inventory

```
Compose from these before writing new markup:
  <DataTable columns={…} rows={…} onRowClick={…} />  — web/src/components/DataTable.jsx — Canonical table. Every list view in Aurora MUST use this, not a raw <table>.
  <MoneyCell cents={…} currency={…} />  — web/src/components/MoneyCell.jsx — All currency in Aurora is stored in cents and rendered through this component.
  <StatusPill status={…} />  — web/src/components/StatusPill.jsx
```

> Aurora Design Language v3 - approved by Brand Council 2019-04. Do not add ad-hoc colours.  _(web/src/styles/tokens.css)_

## 6. Manually injected context

1 document(s) uploaded by an administrator, because the automated
swarm could not reach them.

- **ADR-021 — event bus deferred (chunk-1)** — Status: Accepted. The team evaluated an internal event bus for invoice state changes. It is deferred to Q1. Do not introduce a message broker into the billing monolith this quarter.

---

## How to use this document

1. **Read section 0 first.** Those are human rulings and they beat everything else.
2. Before changing a file, run `find_change_surface` to see what depends on it.
3. Before writing UI, use the components and tokens in section 5 — never invent a hex value.
4. Before committing, re-read the commit convention in section 4. CI enforces it.
5. Ask the knowledge base directly with `query_knowledge` rather than grepping blindly.
